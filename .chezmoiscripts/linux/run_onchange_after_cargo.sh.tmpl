#!/bin/sh

{{- if ne .chezmoi.os "linux" }}
exit 0
{{- end }}

declare -a packages=(
    "cargo-update"
    "topgrade"
    "onefetch"
)

for package in "${packages[@]}"; do
    cargo install $package
done
